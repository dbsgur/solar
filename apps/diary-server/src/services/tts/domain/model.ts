import { Service } from 'typedi';

@Service()
export class TTSModel {
    // constructor(private openAIService: OpenAIService) {}
    // async generateSpeech(inputText: string) {
    //     return await this.openAIService.audio.speech.create({
    //         model: 'tts-1',
    //         voice: 'alloy',
    //         input: inputText,
    //     });
    // }
}
