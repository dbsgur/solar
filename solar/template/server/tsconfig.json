{
    "extends": "../../server-config.json", // NOTE: 만들어질 프로젝트의 상대경로임.
    "compilerOptions": {
        "baseUrl": "src",
        "outDir": "dist"
    },
    "include": ["src"],
    "exclude": ["node_modules"]
}
